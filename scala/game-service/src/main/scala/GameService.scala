package gameService {

  package object gameService {
    def main(args: Array[String]): Unit = {
//      Thread.sleep(2000)
      println("I don't do a whole lot... yet!")
      scala.io.StdIn.readChar()
      println("yo!")
    }
  }
}
